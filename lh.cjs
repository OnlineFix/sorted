const fs=require('fs'); const txt=fs.readFileSync('www.sorted.repair_2026-03-24_00-01-01.report.html', 'utf8'); const idx=txt.indexOf('window.__LIGHTHOUSE_JSON__ = {'); const start=idx+29; const jsonTxt=txt.substring(start, txt.indexOf('};', start)+1); const json=JSON.parse(jsonTxt); console.log(Object.keys(json.categories).map(k=>k+': '+json.categories[k].score)); Object.values(json.audits).filter(a=>a.score!==null&&a.score<1&&a.weight>0).forEach(a=>console.log(a.id, a.score, a.title)); Object.values(json.audits).filter(a=>a.score===null||(a.score<1 && !a.weight)).forEach(a=> { if(a.details && a.details.type==='opportunity') console.log('Opp:', a.id, a.title) });
